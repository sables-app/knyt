import type * as CSS from "csstype";

// The type must be imported and re-exported for API Extractor to work.
// i.e. it can't be re-exported like this: `export type * as CSS from "csstype";`

export type { CSS };
